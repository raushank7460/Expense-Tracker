{
  "name": "expense-tracker-root",
  "version": "1.0.0",
  "private": true,
  "description": "Complete MERN Stack Expense Tracker Web Application",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5",
    "morgan": "^1.10.0",
    "validator": "^13.12.0"
  },
  "keywords": [
    "mern",
    "expense-tracker",
    "finance",
    "react",
    "express",
    "mongodb",
    "vercel"
  ],
  "author": "",
  "license": "MIT"
}
